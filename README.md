# synapx
Privacy-Preserving Secure String Matching using Searchable Symmetric Encryption (SSE). Documents are encrypted with AES-256, keywords are tokenized via HMAC, and search is performed using trapdoors without revealing plaintext data. Includes a priority-aware model with isolated VM deployment for high-sensitivity workloads.
